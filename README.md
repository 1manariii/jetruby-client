# Front-end для взаимодействия с API

## Команды для взаимодействия

In the project directory, you can run:

### `npm start`

Запускает приложение [http://localhost:3000](http://localhost:3000) которое вы увидите в вашем браузере по умолчанию.
